from setuptools import setup, find_packages

setup(
    name='semantic',
    version='0.0.1',
    description='A tool to do semantic queries on large documents',
    author='Alex Monras',
    license='MIT',
    packages=find_packages('src')
)
